// ============================================================
// ゲーム（報酬システム）の設定。数値はすべてここで調整する。
// 2026-08-08 第4回フィードバックで全面リバランス:
// - コインをシンプルに: れんしゅう1文字=+10 / テスト100点=+50（まとめ=+100）
//   （正解ごとの細かい加算・かんそうボーナスは廃止）
// - 育成はスターだけ: スター1個=10コイン、あげるとレベルが上がり姿が変わる
// - 1体をレベルMAXにするにはスター36個=360コイン ≒ れんしゅう7ステージ分
//   （長く遊べて飽きないバランスを狙った初期値。実際の様子で調整する）
// ============================================================

export const GAME_CONFIG = {
  coins: {
    /** 1文字のれんしゅう（5回コース）を完了 */
    practicePerKanji: 10,
    /** ５もんテストで100点 */
    stageTestPerfectBonus: 50,
    /** まとめテストで100点 */
    termTestPerfectBonus: 100,
    /** 復習1問正解 */
    reviewPerCorrect: 2,
    /** はじめてのプレゼント（プロフィール作成時） */
    initialGift: 60,
  },
  star: {
    /** スター1個の値段（コイン） */
    cost: 10,
  },
  levels: {
    /** 仲間の最大レベル（レベル=姿。上がるたびに見た目が変わる） */
    maxLevel: 6,
    /** レベルN→N+1 に必要なスター数（index = N-1）。合計36個 */
    starsPerLevel: [3, 5, 7, 9, 12],
  },
  gacha: {
    /** ガチャ1回の値段 */
    gachaCost: 30,
    /** 仲間に出会える確率（0-1） */
    encounterRate: 0.72,
    /** この回数連続ではずれたら次は必ず出会える（0で無効） */
    pityStreak: 3,
    /** 最初の1回は必ず出会える */
    firstGachaGuaranteed: true,
    /** レア度ごとの出現率（合計1.0） */
    rarityRates: { common: 0.7, rare: 0.25, epic: 0.05 } as Record<string, number>,
    /** すでにいる仲間が出たとき、おみやげにもらえるスター */
    duplicateStarGift: 1,
  },
  review: {
    /** 間隔反復の間隔（日）。正解するたび次の段階へ */
    intervalsDays: [0, 1, 3, 7, 14, 30],
    /** 「今日の復習」に出す最大数 */
    dailyMax: 20,
  },
  /** マスター字数のお知らせマイルストーン（みんな画面） */
  milestones: [10, 20, 50, 80, 100, 150, 200],
  /** 期の表示名（データ設定だけで変更可能） */
  termLabels: ['1学期', '2学期', '3学期'],
} as const

export type GameConfig = typeof GAME_CONFIG
